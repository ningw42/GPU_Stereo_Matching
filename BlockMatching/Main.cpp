#include "Caller.h"

int main()
{
	//cameraTest();
	start_gpu(Size(320, 240), 5, 64);
	//streamDepth(Size(320, 240), 5, 32);
	//start_cpu(Size(320, 240), 2, 32);
	//start_opencv(Size(320, 240), 5, 64);
	//Calib();
	//cameraTest();
	//remapTest();
	return 0;
}