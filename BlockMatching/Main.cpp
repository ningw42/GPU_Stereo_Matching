#include "Caller.h"

int main()
{
	//cameraTest();
	start_gpu(Size(320, 240), 2, 64);
	//streamDepth(Size(320, 240), 5, 32);
	//start_cpu(Size(320, 240), 5, 48);
	//start_opencv(Size(320, 240), 5, 48);
	//Calib();
	//cameraTest();
	//remapTest();
	return 0;
}